# Dependencies
node_modules/

# Build output
dist/
*.local

# Vite / logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# Env / secrets (OAuth client IDs, API keys go in env, never committed)
.env
.env.*
!.env.example

release/

# CDP / E2E scratch artifacts
.playwright-mcp/
build/_preview*.png
test-results/
playwright-report/
blob-report/
.last-run.json

# SDD scratch (briefs/reports/ledger)
.superpowers/

# QA seed data (disposable; regenerate with `npm run qa:seed`)
qa-workspace/

# App-generated process-docs convention file (written into WORKING folders by
# ensureAgentsFile); not a repo artifact — appears here only if the repo root is
# opened as a working folder.
AGENTS.md
